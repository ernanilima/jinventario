<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item android:title="Inicio"
            android:id="@+id/nav_inicio"
            android:icon="@drawable/ic_inicio" />

        <item android:title="Nova Contagem"
            android:id="@+id/nav_contagem"
            android:icon="@drawable/ic_novo" />
    </group>

    <item android:title="Outros">
        <menu>
            <item android:title="Política de Privacidade"
                android:id="@+id/nav_politica_privacidade"
                android:icon="@drawable/ic_politica_privacidade" />

            <item android:title="Sair"
                android:id="@+id/nav_sair"
                android:icon="@drawable/ic_sair" />
        </menu>
    </item>
</menu>